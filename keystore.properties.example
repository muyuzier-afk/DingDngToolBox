# 复制为仓库根目录的 keystore.properties 后填写（keystore.properties 已被 gitignore）
# 也可在 CI 中通过同名环境变量注入，无需本文件。

storeFile=/absolute/path/to/dingdongji-release.jks
storePassword=your-store-password
keyAlias=dingdongji
keyPassword=your-key-password
